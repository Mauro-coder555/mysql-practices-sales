-- ¿Se podrían realizar las consultas anteriores con NATURAL LEFT JOIN o NATURAL RIGHT JOIN? Justifique su respuesta.

-- "Sí se podría usar NATURAL JOIN técnicamente, pero no se debería" porque:

--  Introduce riesgos innecesarios
--  Hace el código más frágil
--  No ofrece ninguna ventaja real sobre la sintaxis explícita
--  Puede producir resultados inesperados si la estructura de la base cambia